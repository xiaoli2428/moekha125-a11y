import React from 'react'
import Header from './components/Header'
import Hero from './components/Hero'
import Features from './components/Features'
import Footer from './components/Footer'

export default function App() {
  return (
    <div className="min-h-screen flex flex-col">
      <Header />
      <      cd /workspaces/moekha125-a11y

      # Delete the problematic workflow files
      rm -f .github/workflows/build.yml .github/workflows/ci-on-main.yml .github/workflows/main.yml .github/workflows/nodejs-ci.yml

      # Commit and push
      git add . && git commit -m "Remove duplicate CI workflows with invalid SHAs" && git push origin main      cd /workspaces/moekha125-a11y

      # Delete the problematic workflow files
      rm -f .github/workflows/build.yml .github/workflows/ci-on-main.yml .github/workflows/main.yml .github/workflows/nodejs-ci.yml

      # Commit and push
      git add . && git commit -m "Remove duplicate CI workflows with invalid SHAs" && git push origin main      cd /workspaces/moekha125-a11y

      # Delete the problematic workflow files
      rm -f .github/workflows/build.yml .github/workflows/ci-on-main.yml .github/workflows/main.yml .github/workflows/nodejs-ci.yml

      # Commit and push
      git add . && git commit -m "Remove duplicate CI workflows with invalid SHAs" && git push origin main      cd /workspaces/moekha125-a11y

      # Delete the problematic workflow files
      rm -f .github/workflows/build.yml .github/workflows/ci-on-main.yml .github/workflows/main.yml .github/workflows/nodejs-ci.yml

      # Commit and push
      git add . && git commit -m "Remove duplicate CI workflows with invalid SHAs" && git push origin mainmain className="flex-1">
        <Hero />
        <Features />
      </main>
      <Footer />
    </div>
  )
}
